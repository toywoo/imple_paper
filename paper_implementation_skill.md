# 스킬: 논문 구현용 코드 스켈레톤 생성 (Paper-to-Skeleton)

## 목적
새로운 딥러닝/머신러닝 논문이나 모델 구조를 처음부터 구현(from scratch)할 때, 작성자가 아키텍처의 전체 흐름과 각 모듈의 역할을 명확히 이해하면서 코드를 작성할 수 있도록 돕는 **가이드형 코드 스켈레톤**을 생성하는 프롬프트/가이드 스킬입니다. 향후 다른 논문을 구현할 때 이 스킬(규칙과 템플릿)을 참조하여 뼈대 코드를 만들어 달라고 요청하면 됩니다.

---

## 스켈레톤 생성 규칙 (가이드라인)

이 스킬을 사용하여 새로운 주제(논문)에 대한 스켈레톤을 생성할 때는 반드시 다음 구조와 규칙을 따릅니다.

### 1. 파일 헤더 (전체 개요)
- **논문 정보**: 제목, 저자, 발표 연도 및 출처 등
- **핵심 아이디어**: 논문/모델의 가장 중요한 기술적 기여와 특징 (2~3가지 요약)
- **전체 아키텍처 흐름 (Data Flow)**: 입력(Input)부터 출력(Output)까지 텐서(Tensor)의 shape이 각 단계를 거치며 어떻게 변하는지 직관적으로 도식화
- **기본 하이퍼파라미터**: 논문에서 제시하는 기본 모델(Tiny, Base 등) 설정값

### 2. 모듈화 및 단계적 분할 (Step-by-Step)
- 전체 모델을 논리적이고 의미 있는 서브 모듈 단위로 쪼개어 `Step 1`, `Step 2` 등으로 넘버링합니다.
- 각 모듈은 `nn.Module`을 상속받는 클래스(또는 유틸리티 함수)로 선언합니다.

### 3. 상세한 Docstring 및 구현 힌트 제공
정답 코드를 바로 주지 않고, 각 클래스/함수 내부에 다음 내용을 포함하는 주석(Docstring)을 작성하여 학습을 유도합니다.
- **모듈의 역할**: 이 모듈이 전체 아키텍처에서 어떤 기능을 하는지 설명
- **핵심 개념 / 기존 모델과의 차이점**: 필요 시 수식이나 개념 설명 추가
- **[구현 힌트]**: 
  - 사용할 PyTorch API (`nn.Conv2d`, `torch.roll`, `einsum` 등) 추천
  - 차원 변경(reshape, permute, view) 및 연산의 논리적 순서 제시
- **Args 및 Returns**: 입력 텐서와 출력 텐서의 **정확한 Shape** (예: `(B, C, H, W)`)을 반드시 명시하여 텐서 크기 불일치 오류를 예방

### 4. 정답 코드 대신 TODO 마커 사용
- `__init__`과 `forward` 메서드 내부에는 `pass`를 남겨둡니다.
- 작성자가 직접 구현해야 할 부분에 구체적인 행동을 지시하는 `# TODO: ...` 주석을 단계별로 달아줍니다.

### 5. 테스트 및 검증 코드 블록
- 파일의 맨 마지막에는 `if __name__ == "__main__":` 블록을 추가합니다.
- 더미 텐서(`torch.randn`)를 이용한 Forward pass shape 테스트 코드와 파라미터 수(Parameter count) 계산 코드를 주석 처리하여 제공합니다.

---

## Python 스켈레톤 템플릿 (예시)

```python
"""
=============================================================================
[모델명] 구현 가이드
Paper: "[논문 제목]" ([저자], [연도])
=============================================================================

[핵심 아이디어]
- (예: 기존 모델의 어떤 한계를 어떻게 극복했는지)
- ...

[전체 아키텍처 흐름]
  Input Image (B, 3, H, W)
       ↓
  ① Step 1: [모듈명]  →  (B, C', H', W')
       ↓
  ② Step 2: [모듈명]  →  (B, C'', H'', W'')
       ↓
  ...
  최종 Output         →  (B, num_classes)

[기본 하이퍼파라미터]
  - ...
"""

import torch
import torch.nn as nn
import torch.nn.functional as F

# =============================================================================
# Step 1: [모듈 이름]
# =============================================================================
class ModuleName(nn.Module):
    """
    [모듈의 역할 설명]

    [구현 힌트]
    - 1. ...
    - 2. ...
    """
    def __init__(self, ...):
        super().__init__()
        # TODO: 필요한 레이어 정의
        pass

    def forward(self, x):
        """
        Args:
            x: (B, ...) - 입력 설명
        Returns:
            (B, ...) - 출력 설명
        """
        # TODO: 레이어 연결 및 forward 연산 흐름 작성
        pass

# ... (Step 2, Step 3 반복) ...

# =============================================================================
# Step N: 전체 모델 조립
# =============================================================================
class FullModel(nn.Module):
    """
    전체 아키텍처를 조립하는 메인 클래스.
    """
    def __init__(self, ...):
        super().__init__()
        # TODO: Step 1~N-1 에서 만든 모듈들을 인스턴스화하여 연결
        pass

    def forward(self, x):
        # TODO: 전체 forward 흐름 구현
        pass

# =============================================================================
# 테스트 코드 (구현 완료 후 주석 해제하여 확인)
# =============================================================================
if __name__ == "__main__":
    # x = torch.randn(2, ...)
    # model = FullModel(...)
    # out = model(x)
    # print("Output shape:", out.shape)
    
    # total_params = sum(p.numel() for p in model.parameters())
    # print(f"Total parameters: {total_params / 1e6:.1f}M")
    pass
```
